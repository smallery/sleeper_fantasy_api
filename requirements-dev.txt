# Development dependencies
# Install with: pip install -r requirements-dev.txt

# Core dependencies (from requirements.txt)
-r requirements.txt

# Testing
pytest>=8.2.2
pytest-cov>=5.0.0
pytest-xdist>=3.6.1
pytest-mock>=3.14.0
coverage>=7.5.0

# Code Quality
black>=24.4.2
isort>=5.13.2
flake8>=7.0.0
mypy>=1.10.0
types-requests>=2.31.0

# Security
bandit>=1.7.8
safety>=3.2.0
# pip-audit requires platformdirs>=4.2.0 -- it could not be installed alongside
# this package until the platformdirs==3.8.1 runtime pin was lifted in 0.4.0.
pip-audit>=2.7.0
